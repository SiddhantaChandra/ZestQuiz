[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate deploy && echo 'Migrations complete, starting NestJS application on port ${PORT:-3001}...' && node dist/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.envs]
NODE_ENV = "production"